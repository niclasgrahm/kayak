---
layout: page
title: kayak
titleTemplate: stream processing you can watch
sidebar: false
outline: false
aside: false
footer: false
pageClass: landing-page
---

<!--
  The landing page. Everything on it is in `.vitepress/theme/Landing.vue` —
  the copy, the pipeline-card replica, the scroll-driven tour — because the
  page is a component, not a document. `layout: page` keeps the site's nav,
  `sidebar: false` keeps the guide's tree off it; `footer: false` because the page ends on its own.
-->

<Landing />
